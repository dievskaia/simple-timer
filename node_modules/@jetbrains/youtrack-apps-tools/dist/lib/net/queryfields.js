"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.queryfields = queryfields;
function queryfields(it) {
    if (it === undefined) {
        return '';
    }
    if (typeof it === 'string') {
        return it;
    }
    if (Array.isArray(it)) {
        return it.map(i => queryfields(i)).join(',');
    }
    if (it !== null && typeof it === 'object') {
        return queryfields(Object.keys(it).reduce((result, i) => {
            return result.concat(i + '(' + queryfields(it[i]) + ')');
        }, []));
    }
    return '';
}
