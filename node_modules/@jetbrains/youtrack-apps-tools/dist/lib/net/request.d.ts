import { Config } from '../../@types/types';
export declare function generateRequestParams(config: Config, url: string, options?: {
    method?: string;
    headers?: Record<string, string>;
    body?: FormData;
}): Request;
export declare function prepareErrorMessage(res: Response): Promise<string>;
