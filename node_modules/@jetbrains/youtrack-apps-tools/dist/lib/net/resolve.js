"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolve = resolve;
function resolve(url, a) {
    if (typeof url === 'string' && !/^http(s)?:\/\//.test(url)) {
        url = `https://${url}`;
    }
    if (a && !/\/$/.test(url)) {
        url += '/';
    }
    if (a) {
        a = a.replace(/^\//, '');
    }
    return new URL(a, url);
}
