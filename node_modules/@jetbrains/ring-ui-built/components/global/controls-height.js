import { createContext } from 'react';
import deprecate from 'util-deprecate';
import { ControlsHeight, getConfiguration, configure } from './configuration.js';

// This can be used if React Context is not applicable, for example for alertService or Auth dialog
const configureGlobalControlsHeight = deprecate(value => {
  configure({
    controlsHeight: value
  });
}, 'Ring UI: configureGlobalControlsHeight() is deprecated, use configure() instead');
function getGlobalControlsHeight() {
  var _getConfiguration$con;
  return (_getConfiguration$con = getConfiguration().controlsHeight) !== null && _getConfiguration$con !== void 0 ? _getConfiguration$con : ControlsHeight.M;
}
const ControlsHeightContext = /*#__PURE__*/createContext(getGlobalControlsHeight);

export { ControlsHeight, ControlsHeightContext, configureGlobalControlsHeight, getGlobalControlsHeight };
